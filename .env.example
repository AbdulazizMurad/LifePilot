# Copy this file to .env.local and fill in the values.
# Only VITE_ variables are exposed to the browser — never put secret keys here.

# Supabase (safe to expose — protected by Row Level Security)
VITE_SUPABASE_URL=https://lzqpfneawbinergqmlfj.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_OFsIs9uwACfACuJzPfluBA_WlaxGi-F

# The OpenRouter key is NOT here. It lives only as a Supabase Edge Function
# secret (server-side) so it is never shipped to the browser.
